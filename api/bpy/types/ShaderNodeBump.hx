package bpy.types;
@:pythonImport("bpy.types") extern class ShaderNodeBump extends ShaderNode {
	/**
		Invert the bump mapping direction to push into the surface instead of out
	**/
	public var invert : Bool;
}